<!DOCTYPE html>
<html lang="en">

    <%- include('../partials/head.ejs',{ pageName :  "New Photo" }) %>

<body>

    <h1>Add a photo to your page!</h1>
    <form class="form" method="POST" action="/user/photos/">
            <!-- method => POST - attach the data to post body rather than in the url query string(GET request) -->
            <!-- action => destination for the post req -->
            <div class="form-input">
                <label for="image"> Photo (url): </label>
                <input id="image" name="image" type="text" />
            </div>
            <div class="form-input">
                <label for="caption"> Caption: </label>
                <input id="caption" name="caption" type="text" />
            </div>
            <div class="form-input">
                <label for="user"></label>
                <input id="user" name="user" type="hidden" value="<%= id %>"/>
            </div>
            <input type="submit" value="Add Photo!">
        </form>
</body>
</html>